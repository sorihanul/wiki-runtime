# _runtime_state

This folder stores generated cache and runtime-state files.

Examples:

- cycle caches
- compile state cache
- queue report caches
- lock files

These files help the runtime run efficiently, but they are not public source material.

## Public Rule

- keep the folder for structure
- do not publish cache contents as if they were source
- regenerate locally when needed
