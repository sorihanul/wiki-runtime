# vendor

Use this folder when you want to pin an external dependency script locally inside the repository.

Default use:

- If `ivk2_improved.py` is placed here, `wiki_runtime.py` will detect it automatically.

Recommended filename:

- `ivk2_improved.py`
