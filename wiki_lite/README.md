# wiki_lite

`./wiki_lite` is the lightweight wiki workspace for processing new material quickly.

The default flow is:

1. Place new material in `RAW`.
2. Put lightweight structured results in `WIKI`.
3. Leave decision traces in `LOG`.
4. Promote to `wiki` only if the material has formal long-term value.

The key rule is simple: do not push everything into canon.
